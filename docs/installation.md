# Installation
Koltic offers two installation methods to meet all your needs and preferences: NPM or Yarn, both global and local.
## For packages in NPM
```bash
# Install globally
npm install -g koltic

# Install locally
npm install -D koltic
```
## For packages in Yarn
```bash
# Install globally
yarn global add koltic

# Install locally
yarn add koltic
```